/// GDX Sound Driver (stub - depends on LibGDX/OpenAL).
///
/// Translated from: GdxSoundDriver.java
/// This is a stub since LibGDX integration is deferred.
/// In Rust, Bevy or kira will replace LibGDX for audio.
pub struct GdxSoundDriver {
    // stub
}

impl GdxSoundDriver {
    pub fn new(_song_resource_gen: i32) -> Self {
        todo!("GdxSoundDriver depends on LibGDX (OpenAL)")
    }
}
