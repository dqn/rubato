/// PortAudio Driver (stub - depends on PortAudio native library).
///
/// Translated from: PortAudioDriver.java
/// This is a stub since PortAudio integration is deferred.
pub struct PortAudioDriver {
    // stub
}

impl PortAudioDriver {
    pub fn new(_song_resource_gen: i32) -> Self {
        todo!("PortAudioDriver depends on PortAudio native library")
    }
}
