/// GDX Audio Device Driver (stub - depends on LibGDX).
///
/// Translated from: GdxAudioDeviceDriver.java
/// This is a stub since LibGDX integration is deferred.
pub struct GdxAudioDeviceDriver {
    // stub
}

impl GdxAudioDeviceDriver {
    pub fn new(_song_resource_gen: i32) -> Self {
        todo!("GdxAudioDeviceDriver depends on LibGDX")
    }
}
